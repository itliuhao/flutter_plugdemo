import 'package:flutter/material.dart';

class PluginTest extends StatelessWidget {
  const PluginTest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('测试'),
      ),
    );
  }
}
